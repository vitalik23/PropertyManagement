@using PropertyManagement.Models.UnitViewModels
@model UnitFormViewModel

<form data-modal-form data-refresh-url="/Units/List?propertyId=@Model.PropertyId" data-refresh-target="#units-list" asp-controller="Units" asp-action="@(Model.Id.HasValue ? "Edit" : "Create")" method="post">
    <div class="modal-header">
        <h5 class="modal-title">@(Model.Id.HasValue ? "Edit Unit" : "Add Unit")</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
    </div>
    <div class="modal-body">
        <input type="hidden" asp-for="PropertyId" />
        @if (Model.Id.HasValue)
        {
            <input type="hidden" asp-for="Id" />
        }
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

        <div class="mb-3">
            <label asp-for="UnitNumber" class="form-label"></label>
            <input asp-for="UnitNumber" class="form-control" />
            <span asp-validation-for="UnitNumber" class="text-danger"></span>
        </div>
        <div class="mb-3">
            <label asp-for="Bedrooms" class="form-label"></label>
            <input asp-for="Bedrooms" class="form-control" />
            <span asp-validation-for="Bedrooms" class="text-danger"></span>
        </div>
        <div class="mb-3">
            <label asp-for="MonthlyRent" class="form-label"></label>
            <input asp-for="MonthlyRent" class="form-control" />
            <span asp-validation-for="MonthlyRent" class="text-danger"></span>
        </div>
        <div class="mb-3">
            <label asp-for="UnitTypeId" class="form-label"></label>
            <select asp-for="UnitTypeId" asp-items="Model.UnitTypeOptions" class="form-select">
                <option value="">-- Select a unit type --</option>
            </select>
            <span asp-validation-for="UnitTypeId" class="text-danger"></span>
        </div>
    </div>
    <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
        <button type="submit" class="btn btn-primary">Save</button>
    </div>
</form>
